// Placeholder page so the typed-routes Link in PondHero compiles.
// Phase 3 replaces this with the real FroglingsWorkspace.
export default function FroglingsPage() {
  return (
    <main className="mx-auto w-full max-w-[1180px] px-4 py-10">
      <h1 className="text-2xl font-black text-pond">Funner version — coming soon</h1>
      <p className="mt-2 text-sm text-ink/70">
        This page will host the kid-friendly Debatefrog. We're still hopping it together.
      </p>
    </main>
  );
}
